import { createTheme } from '@mui/material/styles';

export const lightTheme = createTheme({
    palette: {
      mode: 'light',
      // define your light mode palette here
    },
  });
  
  export const darkTheme = createTheme({
    palette: {
      mode: 'dark',
      // define your dark mode palette here
    },
  });